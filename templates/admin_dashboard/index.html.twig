{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}

 <h1>DashBoard administrateur</h1>

    <div class="d-flex justify-content-start ">
        <div class="mr-2">
            <a href={{ path('app_admin_user_index') }} class="btn btn-primary" type="button">Liste des utilisateurs</a>
        </div>
        <div class="mx-2">
            <a href={{ path('app_admin_category_index') }}  class="btn btn-primary" type="button">Liste des catégories</a>
        </div>
        <div>
            <a href={{ path('app_admin_post_index') }}  class="btn btn-primary mx-2" type="button">Liste des articles</a>
        </div>
        <div>
            <a href={{ path('app_admin_register') }}  class="btn btn-outline-dark" type="button">Ajouter un administrateur</a>
        </div>
    </div>
   
{% endblock %}
